describe('Find the maximum', () => {
  /*
  it('should declare a function named updateSubtotal', () => {
    expect(typeof updateSubtotal).toBe('function');
  });
  
  it('should return a number', () => {
    expect(typeof updateSubtotal(1)).toEqual('number');
  });
  */
});
